# Codex Scratchpad

Last updated: 2026-05-26

Purpose: This is the working handoff between Codex, Chad, ChatGPT, and any design/product thinking around Nova Notetaker. Keep this file current with project status, design choices, implementation decisions, known issues, and open questions.

## Project Status

Nova Notetaker is a native Windows desktop app built with Python and PySide6. It passively captures meeting audio from the local machine rather than joining Teams or using a Teams admin integration.

Current core flow:

- Capture system/output audio through WASAPI loopback.
- Optionally capture microphone audio.
- Save raw audio into a timestamped meeting folder.
- Transcribe audio through self-hosted WhisperLive.
- Generate notes through self-hosted Ollama.
- Write `notes.md`, `transcript.md`, `metadata.json`, and now `insights.json`.
- Review saved meetings from the Meetings tab.
- Review follow-up dates and action items from the Calendar workspace.
- Open a selected meeting either in the in-app workspace or in a separate popout overview.

Current infrastructure assumptions:

- WhisperLive runs on Chad's internal Unraid server.
- Ollama runs on Chad's internal Unraid server.
- Kokoro FastAPI and n8n exist internally but are not core dependencies yet.
- The app is intended to work passively with Teams, browser audio, YouTube test audio, or any Windows system output.

## Recent Engineering Decisions

- System audio now defaults to `Windows default output`.
  - This avoids stale output-device selection when Windows switches between laptop speakers, Realtek output, AirPods, etc.
  - The app resolves the active WASAPI loopback at recording start.

- Microphone capture now defaults to `Windows default microphone`.
  - If Windows reports the default mic through a less ideal/truncated device name, Nova tries to prefer the matching visible WASAPI mic.

- Mic capture can be muted explicitly.
  - The UI state is `Mic ON` or `Mic MUTED`.
  - When muted, `mic.wav` is intentionally skipped.

- Long WhisperLive transcriptions are chunked.
  - Long recordings are split into temporary WAV chunks.
  - Default chunk size is 180 seconds.
  - Each chunk gets a separate WhisperLive WebSocket connection with retry.
  - This is intended to reduce `[WinError 10053]` connection abort failures on 10+ minute recordings.

- Meeting insights are now structured.
  - `insights.json` is generated for newly processed/reprocessed meetings.
  - Existing meetings without `insights.json` can still be parsed from `notes.md`.
  - `insights.json` is rebuilt when `notes.md` is newer, while preserving matching action-item statuses.
  - Decision parsing filters obvious assignment/deadline bullets so the overview does not treat every commitment as a decision.

- Meeting profiles are implemented and editable.
  - Profiles live in `config/profiles.json`.
  - Live Capture has a profile selector.
  - Profiles can define a default note template.
  - Changing the Meeting Profile on Live Capture loads that profile's default Note Template when one is configured.
  - Manually changing the Note Template on Live Capture only changes the current/default app selection; it does not update the saved profile.
  - Selected profile data is written into meeting metadata.
  - Ollama receives profile context during note generation.
  - The Templates page includes a Meeting Profiles editor with create, edit, save, and delete workflows.
  - Reprocess now lets the user override the meeting profile before regenerating notes or doing a full reprocess.

- Note templates are implemented as an MVP.
  - Templates live in `config/templates.json`.
  - Live Capture has a template selector under the meeting profile selector.
  - The Templates tab now supports create, edit, save, and delete workflows.
  - Selected template data is written into meeting metadata.
  - Ollama receives template context during note generation.
  - Templates are for output style and structure; profiles are for meeting/company context.
  - Reprocess now lets the user override the note template before regenerating notes or doing a full reprocess.

- Ollama notes generation now has a format guardrail.
  - Nova validates that generated notes include the required sections:
    - Summary
    - Key Decisions
    - Action Items
    - Important Dates
    - Risks / Blockers
    - Follow-ups
  - If Ollama returns prose or misses required sections, Nova retries once with a stricter rewrite prompt.
  - If the retry still misses the format, Nova keeps the notes but records a processing warning.

- Reprocess transcript handling was fixed.
  - System-audio-only meetings often have `## You` marked as `_Transcript pending._`.
  - Notes-only reprocess now ignores pending sections and processing warnings, then uses any real transcript text that remains.
  - This prevents valid meeting-audio transcripts from being replaced by the "pending transcription" notes stub.

## UI / Design Direction

The current design direction is a corporate dark executive dashboard, not a sci-fi HUD.

Primary design language:

- Matte dark surfaces.
- Restrained orange accents.
- Practical enterprise dashboard layout.
- Clear hierarchy over visual spectacle.
- Minimal glow, mostly reserved for the Nova orb/status identity.

Current UI structure:

- Left sidebar navigation:
  - Live Capture
  - Meetings
  - Search
  - Calendar
  - Logs
  - Templates
  - Settings

- Live Capture page:
  - Header with theme toggle.
  - Meeting profile selector.
  - Note template selector.
  - Workflow-oriented meeting setup and current-state card.
  - Audio levels.
  - Mic state pill.
  - Live transcript panel with near-realtime WhisperLive rolling transcript when available.
  - Bottom action dock with Start/Stop, Open latest, Mark Important, and Add Note.
  - Meeting Insights rail with operational metrics for open actions, missing owners, due-soon items, and review warnings.
  - Timer shows recording duration during capture, then resets and shows processing duration after stop.

- Meetings page:
  - Clean table/index only.
  - Date, time, meeting name, status, open action count, review count, Open popout action, and Workspace action.
  - Selected meetings can be deleted.
  - Batch reprocess and exports are available for selected meetings.
  - Table styling has been normalized to avoid heavy selected-row bands, orange cell slivers, and focus artifacts.

- Search page:
  - Searches meeting titles, notes, transcripts, owners, and dates.
  - Search results open meetings in the in-app workspace.

- Calendar page:
  - Dates tab reviews detected calendar candidates by meeting.
  - The month calendar now has a larger dedicated layout region instead of being squeezed beside the table.
  - Detected date items render directly on the calendar as day count badges; approved days get an orange visual cue.
  - Selecting a highlighted date shows a compact summary below the calendar and selects the matching table row.
  - Calendar candidates have an `Approved` checkbox.
  - Date/context cells are editable and saved to per-meeting `calendar_review.json`.
  - Export visible dates exports approved candidates only.
  - Actions tab lists cross-meeting action items with owner/status/confidence filters.
  - Action status can be updated directly from the table.

- Templates page:
  - Split into two vertical sections:
    - Meeting profiles on top.
    - Note templates below.
  - Meeting profiles edit name, category, company context, title prefix, AI context, and notes focus.
  - Note templates edit name, category, description, preferred sections, notes focus, and custom instructions.
  - New/save/delete changes are saved locally and reflected in the Live Capture selectors.
  - Layout was tightened after screenshot review:
    - Inner section headings are smaller.
    - Child panels use transparent backgrounds to avoid harsh black blocks.
    - Tables and text fields use more compact heights.
    - On compact/narrow windows, each section stacks the table above its editor.

- Settings page:
  - Settings content now sits inside a scroll area.
  - Form controls have minimum heights so they remain readable when the window is shortened.
  - This avoids the previous behavior where fields flattened or disappeared on smaller window sizes.
  - Settings now include compact helper text and tooltips for device selection, capture profiles, Ollama settings, WhisperLive model/language/VAD/cleanup, timeouts, and long-recording chunk size.
  - Whisper model guidance currently names common values such as `tiny`, `base`, `small`, `medium`, and `large-v3`, with a note that actual availability depends on the WhisperLive container/config.

- Meeting overview popout:
  - Rendered notes on the left.
  - Details and structured insights on the right.
  - Popout windows close when the main app closes.
  - Popouts are screen-aware and should stay inside the available display.

- Meeting overview workspace:
  - In-app workspace page exists in the main stack.
  - Meetings can open into the workspace from Meetings, Search, Calendar/Actions, or Open latest.
  - Workspace is now organized as an executive briefing:
    - Executive summary
    - Key decisions
    - Action items
    - Dates / follow-ups
    - Risks / review needed
    - Structured notes
    - Transcript evidence
    - Review controls/details rail
  - Speaker aliases can be edited through Rename Speakers and are applied to transcript display without rewriting `transcript.md`.

## Theme System

Two themes are implemented:

- Executive Dark
  - Current/default theme.
  - Deep charcoal/black surfaces, refined orange accents, professional and focused.
  - Latest polish moved the palette away from blue/slate and toward neutral graphite surfaces.
  - Table selection, headers, and embedded table buttons were softened so Meetings, Search, Calendar, Actions, and Templates read consistently.

- Graphite Light
  - Light theme.
  - Intended for office/client/all-day use.
  - Uses graphite whites and muted charcoal surfaces instead of stark white.

Theme switching:

- Two-way segmented toggle at the top of Live Capture:
  - Dark
  - Light
- Theme preference is saved in `config/settings.json`.
- Meeting overview popouts inherit the active theme.

Design team note:

- Graphite Light is functional but likely needs a visual polish pass after screenshot review.
- Watch text contrast in Markdown previews, tables, disabled controls, and badges.

## Insights Design

Current insight types:

- Action items
- Key decisions
- Scheduling details / dates
- Review needed / quality warnings

Insight fields:

- Owner
- Due date
- Confidence
- Status
- Context/text

Source was removed from visible notes and insight cards because it added clutter without enough value for the current UI.

Current action state:

- Structured action items default to `open`.
- Meeting Overview and Calendar > Actions include status selectors:
  - open
  - in progress
  - done
  - deferred
  - closed
- Status changes are saved back to `insights.json`.
- Actions older than `review.action_auto_close_days` are automatically marked `closed`.
- `review.action_auto_close_days` defaults to 30 and can be set to 0 to disable auto-close.

Potential future insight improvements:

- Add click-to-jump from insight item to the relevant note section.
- Add deeper Outlook/n8n/Planner integration after local review workflows stabilize.

## Known Issues / Watch Items

- WhisperLive may still fail on long meetings if the server is overloaded.
  - Chunking should reduce total failure risk.
  - If failures continue, try shorter chunk size such as 60 or 90 seconds.

- Laptop speakers plus integrated mic can create bleed/confusion.
  - Current workaround is mic mute on laptop.
  - Better long-term solutions may include audio routing guidance, headset mode, or echo cancellation research.

- True speaker diarization is not solved.
  - Current minimum reliable distinction is `You` versus `Meeting/System Audio`.
  - Passive speaker rename aliases are implemented, but Nova does not yet split meeting audio into true speaker segments.

- Meeting overview layout is improving but should keep being screenshot-tested.
  - Especially on laptop screens, high Windows scaling, and multi-monitor setups.

- The legacy `_build_legacy_ui` code still exists in `main_window.py`.
  - It is not called, but it creates noise when searching the file.
  - Cleanup/refactor pass recommended once UI stabilizes.

## Open Product / Design Questions

- Should the app show live partial transcript while recording, or only after processing?
- Should insights appear only after processing, or should we show â€œcapture/transcription stateâ€ cards while recording?
- What is the right visual weight for the Nova orb?
  - Current orb is brand/status flavor.
  - It should not overpower the business dashboard.

- Should action item state be local-only, or synced/exported somewhere?
  - Local-only is simpler.
  - n8n/Outlook/Planner integration would make it more useful.

- Should `Source` be fully removed from generated notes, or kept in `insights.json` only?
  - Current decision: hidden from UI and new notes, but still supported in parser for older notes.

- What should Templates become next?
  - MVP now supports custom templates.
  - Possible next step: import/export template packs and per-template insight-card preferences.
  - Need design guidance on whether Templates should feel like a simple form or a richer "prompt studio" later.

## Next Useful Implementation Ideas

- Add an `insights.json` backfill command for old meetings.
- Remove unused legacy UI code from `main_window.py`.
- Add screenshot-based UI QA notes for Dark and Graphite Light.
- Add Outlook/n8n/Planner integration for approved calendar/action items.
- Add import/export for meeting profiles and note templates.
- Continue splitting `main_window.py` into page/component modules.

## Last Verified State

- Last code verification: 2026-05-26.
- Full test discovery passed after the layout constraint pass:
  - 32 tests OK.
- Latest UI smoke check:
  - Offscreen geometry check at 780x560, 1024x680, and 1366x768 found 0 visible child-boundary violations.
- Most recent feature batch:
  - Calendar approval/edit workflow.
  - In-app meeting overview workspace.
  - Passive speaker rename aliases.
  - Actions folded under Calendar.
  - Layout constraint pass for Meetings, Calendar, Actions, Search, and Settings.

## Current Next Recommendations

- Screenshot-review the updated Calendar/Actions workspace and in-app Meeting Overview at real Windows scaling.
- Continue tightening the Ollama prompt if assignments still leak into Key Decisions in generated notes.
- Test near-realtime WhisperLive transcript streaming against the real server and tune latency/chunk behavior.
- Continue decomposing `main_window.py`; `app/ui/review_helpers.py` is only the first extraction.

## 2026-05-26 Update - Latest Reprocess Review

- Reviewed latest meeting folder:
  - `meetings/2026-05-26_083001_daily-operations-checkpoint`
  - System audio is valid: about 551.6 seconds, 48 kHz, 2 channels.
  - Mic capture was intentionally muted and no mic WAV was expected.
  - Reprocess mode was `notes_only`.

- Reprocess quality:
  - Notes were generated successfully with all required sections.
  - The summary correctly captured two related network/security-camera incidents.
  - Action item extraction found the change-record follow-up for IT Team.
  - Important date extraction found the next-meeting reference.
  - Remaining review warning is valid: one action item has an unknown due date.

- Fixes implemented during this pass:
  - Insights parser now ignores placeholder bullets like `None captured.` so they do not become fake decisions/actions/dates.
  - Regenerated the latest meeting's `insights.json`; it now has 1 action, 0 decisions, 1 date.
  - Meeting table now has a filter control:
    - All meetings
    - Needs review
    - Has open actions
    - Processed
    - No transcript
  - `No transcript` filter checks for usable transcript text, so mic-muted meetings with valid system transcript are not misclassified.
  - Settings page now includes direct diagnostics buttons for Ollama and WhisperLive.
  - Transcript cleanup now collapses incremental/repeated fragments before sending transcript text to the note generator.

- Verification:
  - `py_compile` passed for changed app/test files.
  - Full relevant unit suite passed: 27 tests OK.

- Follow-up item still relevant:
  - Consider expanding the transcript quality panel with transcription chunks, repeated-fragment cleanup count, and note-generation retry status.

## 2026-05-26 Update - Product Polish Pass

- Reviewed `.notes/ChatGPT Scratchpad` and implemented the recommended UI/product cohesion pass.

- Meetings page:
  - Added human-readable status badge presentation.
  - Replaced backend-feeling status labels with:
    - Complete
    - Needs review
    - Processing
    - Draft
    - Missing transcript
  - Updated filter labels to match product language:
    - Complete
    - Missing transcript
  - Status badges now use color/state semantics rather than raw text alone.

- Live Capture:
  - Promoted the timer to a larger hero metric style.
  - Renamed the status area to "Recording state" to make the workflow clearer.

- Meeting Overview:
  - Added an executive summary card above the structured notes when summary bullets are available.
  - Renamed the main notes area to "Structured notes".
  - Reordered the right rail so meeting intelligence appears before technical details:
    - Action items
    - Key decisions
    - Scheduling details
    - Review needed
    - Meeting details
  - This moves the overview closer to an executive briefing instead of a notes/transcript viewer.

- Settings:
  - Converted the settings wall into grouped tabs:
    - Device capture
    - Intelligence
    - Transcription
  - Moved most help text into tooltips.
  - Kept inline helper text only for higher-friction settings such as capture profile, Ollama model, Whisper model, and long recording chunk size.

- Templates:
  - Converted the Templates page into two tabs:
    - Meeting profiles
    - Note templates
  - This keeps the page shorter and avoids pushing the note-template editor below the fold.

- Design system:
  - Added `.notes/UI Design System`.
  - Captures product feel, theme guidance, color usage, typography, spacing, badges, page hierarchy, settings/templates rules, and responsive testing rules.

- Verification:
  - `py_compile` passed for changed UI/workflow files.
  - Relevant unit suite passed: 27 tests OK.

## 2026-05-26 Update - Review Workflow Feature Batch

- Added the next functionality layer requested by Chad:
  - Open latest meeting.
  - Meeting quality panel.
  - Cross-meeting action dashboard.
  - Calendar candidate export.
  - Cross-meeting search.
  - Batch reprocess.

- Live Capture:
  - Added `Open latest meeting` to the action bar.
  - Button opens the newest meeting overview and becomes available once meetings exist.

- Review Center:
  - The Logs page is now a broader Review Center while preserving raw logs.
  - Tabs added:
    - Actions
    - Calendar
    - Search
    - Raw logs
  - Actions tab lists open action items across all meetings with meeting, owner, action text, due date, confidence, and status.
  - Calendar tab lists detected scheduling candidates across all meetings.
  - Search tab searches meeting titles, notes, and transcripts and opens matching meetings.

- Meeting Overview:
  - Added a quality summary card showing recording duration, transcript availability, open action count, and warning count.

- Meetings:
  - Added `Batch reprocess` for selected meetings.
  - Added `Export calendar` for selected meeting dates.

- Calendar export:
  - Exports detected dates as `.ics` VTODO items.
  - This is intentionally review-oriented because many extracted dates are fuzzy natural language such as "Tomorrow" or "Next meeting".

- Batch reprocess:
  - Runs selected meetings in a background worker.
  - Uses the same reprocess dialog so Chad can choose full vs notes-only plus meeting profile and note template.

- Verification:
  - `py_compile` passed for changed files.
  - Relevant unit suite passed: 27 tests OK.

## 2026-05-26 Update - Search / Calendar / Logs Split

- Reworked the former Review Center into separate sidebar pages:
  - Search
  - Calendar
  - Logs
  - Templates
  - Settings

- Search:
  - Pulled out of the old Review Center tab structure.
  - Now has its own first-class sidebar page.

- Calendar:
  - Pulled out of the old Review Center tab structure.
  - Added a meeting filter so detected dates can be viewed by:
    - All meetings
    - One selected meeting
  - `Export visible dates` now respects the selected meeting filter.
  - Added a calendar widget beside the candidate list.
  - Selecting a date row moves the calendar to that date when Nova can parse it.
  - Supported parsing includes ISO dates, month/day formats, and "Tomorrow" relative to meeting start time.

- Logs:
  - Renamed sidebar item back to Logs.
  - Logs now sits above Templates.
  - It contains only raw capture/transcription/processing log output.

- Verification:
  - `py_compile` passed for `app/ui/main_window.py`.
  - Relevant unit suite passed: 27 tests OK.

## Communication Notes For ChatGPT / Design Team

When giving design feedback, focus on:

- Visual hierarchy.
- Readability at 1366x768 and laptop scale.
- Whether the insights rail feels helpful or crowded.
- Whether Graphite Light feels professional enough.
- How much Nova brand personality is appropriate without feeling like a sci-fi assistant.

When giving product feedback, focus on:

- How a user starts/stops capture.
- How confidently the app communicates recording/transcription status.
- How users review and act on previous meetings.
- Whether the overview popout or an in-app overview page is the better long-term design.

## 2026-05-26 Update - Functional Roadmap Batch 1

- Started implementing the "next recommendations" feature set from the new chat.
- Added a first-class Actions sidebar page:
  - Follow-up refinement: Actions was moved under the Calendar page as an `Actions` tab instead of remaining a standalone sidebar item.
  - Filters by owner, status, and confidence.
  - Shows cross-meeting action items.
  - Lets action status be updated directly from the table.
  - Opens the source meeting overview from each action row.
- Wired the previously disabled Live Capture marker controls:
  - `Mark Important` writes an important marker while recording.
  - `Add Note` writes a timestamped free-text marker while recording.
  - Markers are saved as `markers.json` in the meeting folder.
  - Meeting Overview now shows a Meeting markers card.
- Added a Settings `Run preflight` button:
  - Checks microphone/default capture state.
  - Checks WASAPI loopback resolution.
  - Checks meeting storage writability.
  - Checks Ollama reachability/model presence.
  - Checks WhisperLive WebSocket reachability when transcription is enabled.
- Added template/profile usability helpers:
  - Duplicate meeting profile.
  - Preview meeting profile prompt context.
  - Duplicate note template.
  - Preview note template prompt context.
- Added lightweight meeting index foundation:
  - New `app/storage/meeting_index.py`.
  - Writes `meetings/index.json` during meeting refresh.
  - Captures title, status, transcript availability, open action count, review count, owners, dates, and searchable text.
- Added action lifecycle setting:
  - `config/settings.json` now has `review.action_auto_close_days`, default `30`.
  - Settings has a Review tab where this can be changed.
  - Actions older than the configured meeting age are marked `closed`.
  - `0` disables auto-close.
- Verification:
  - `py_compile` passed for changed app files.
  - Full test discovery passed: 30 tests OK.
  - Offscreen UI instantiation succeeded with 7 pages after Actions moved under Calendar.

Remaining roadmap items:

- True near-realtime WhisperLive transcript streaming is still pending.
- `main_window.py` still needs more page/component extraction before the next large UI expansion.

## 2026-05-26 Update - Session Finish Pass

- Calendar approval/edit workflow:
  - Calendar date rows now include an `Approved` checkbox.
  - Date and context are editable directly in the table.
  - Review edits are persisted per meeting in `calendar_review.json`.
  - Calendar `.ics` export now exports approved candidates only.
- Calendar/Actions information architecture:
  - Actions remains inside the Calendar workspace as an `Actions` tab.
  - Calendar now acts as the broader follow-up workspace for dates and action items.
- In-app Meeting Overview workspace:
  - Added a hidden workspace page in the main stack.
  - Meetings table now has `Open` popout and `Workspace` buttons.
  - Search and Actions open meetings into the workspace by default.
  - Workspace includes Structured notes, Transcript, and Details tabs.
- Passive speaker rename:
  - Added speaker alias support backed by per-meeting `speaker_aliases.json`.
  - Rename Speakers prompts for transcript headings such as `You` and `Meeting`.
  - Aliases are applied to transcript display without rewriting raw `transcript.md`.
- Main-window split:
  - Added `app/ui/review_helpers.py` for calendar review keys/state, speaker aliases, and ICS/string helpers.
  - This is the first extraction from `main_window.py`; more UI page extraction is still recommended later.
- Verification:
  - `py_compile` passed for changed app files.
  - Full test discovery passed: 32 tests OK.
  - Offscreen UI smoke check succeeded with 8 stacked pages and workspace navigation.

## 2026-05-26 Update - Layout Constraint Pass

- Reviewed page layouts for elements exceeding boundaries after the Calendar/Actions/Workspace additions.
- Tightened fixed-width pressure points:
  - Meetings table action/status columns reduced and constrained.
  - Meetings bottom command buttons now flow through a compact grid instead of one long horizontal row.
  - Actions filters now use a grid layout.
  - Actions table fixed columns were reduced and horizontal scrolling is kept internal to the table.
  - Calendar date review table and filter/export controls now allow shrinkage.
  - Calendar widget minimum width reduced for narrower windows.
  - Search results table now allows internal horizontal scrolling.
  - Settings command buttons now use expanding sizing instead of forcing a wide row.
- Reduced table/header padding slightly in `app/ui/styles.py`.
- Verification:
  - Offscreen geometry smoke check at 780x560, 1024x680, and 1366x768 found 0 visible child-boundary violations.
  - `py_compile` passed for changed UI files.
  - Full test discovery passed: 32 tests OK.

